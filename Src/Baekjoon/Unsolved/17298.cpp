#include <iostream>
#include <stack>
using namespace std;

int main(int argc, char* argv[]) {
  /* Faster */
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  /* Init */

  /* End */
  return 0;
}

/* 이중 for문을 이용한 방식 사용 시, 시간복잡도가 O(n^2)으로, 시간초과가
 * 발생하게 된다. */
